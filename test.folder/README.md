# Test Folder

Running `pytest` generates a new `run_<UTC timestamp>` folder here with phase-by-phase host screen captures (`.html`), API state snapshots (`.json`), and a `SUMMARY.md` report of what was captured.
